#include <stdio.h>
/**
 * main - Entry point. Prints all digits of base 0 withou
 * Return: Always(0) Success
 */
int main(void)
{
	int l;

	for (l = 48; l < 58; l++)
	{
		putchar(l);
	}
	putchar('\n');

	return (0);
}
