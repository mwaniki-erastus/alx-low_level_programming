#include <stdio.h>
int main(void) 
{
/* main - entry point
 * A program that uses puts to print a string.
 * puts normally prints a new line after printing the cgstring
 * Return - Always 0 (Success)
 */
char string[] = "Programming is like building a multilingual puzzle";
puts(string);
return (0);
}		
