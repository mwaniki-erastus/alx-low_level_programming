#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: return string
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int k;

	for (k = 0; s[k] != 0; k++)
	{
	}

	return (k);
}

