#include "main.h"
/**
  * _isupper - checks for uppercase
  * @c: character to be checked
  * Return: 1(u) or 0(otherwise)
  */

int _isupper(char c)
{
	if isupper(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

