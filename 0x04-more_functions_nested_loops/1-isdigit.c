#include "main.h"

/**
  * _isdigit - checks if passed character is an int
  * @c: character to be checked
  * Return: 1(digit) 0(otherwise)
  */

int _isdigit(int c)
{
	if isdigit(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
