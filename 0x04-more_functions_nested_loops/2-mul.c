#include "main.h"

/**
 * mul - multiplies two integers and returns the product
 * @a: first integer
 * @b: second integer
 * Return: the product of the 2 integer inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
