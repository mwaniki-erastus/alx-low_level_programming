#include <stdio.h>
/**
 * main - entry point
 * @argc: argument count
 * @argv: Argument Vector
 * A program that prints the number or arguments passed
 * Return: 0 success
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
