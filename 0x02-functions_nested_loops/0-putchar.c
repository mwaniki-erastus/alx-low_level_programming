#include "main.h"
#include <unistd.h>
#include <string.h>
/**
 * main - Entry point during execution
 * iProgram that prints all single digits of base 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	write(1, "_putchar", strlen("_putchar"));
	putchar(10);
	return (0);
}
